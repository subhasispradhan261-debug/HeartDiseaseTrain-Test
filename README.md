# HeartDiseaseTrain-Test
The dataset is split into training and testing sets to build a reliable Heart Disease Detection model. The training data helps the model learn patterns, while the test data evaluates performance on unseen cases, ensuring accuracy and generalization.
